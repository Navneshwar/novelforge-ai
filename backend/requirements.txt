# Core Framework
fastapi>=0.116.2
uvicorn[standard]>=0.30.1
python-dotenv>=1.0.1

# Database
sqlalchemy>=2.0.31

# Cognee - Memory Layer
# Pinned to the range verified against memory_service.py's exact kwargs
# (remember/recall/forget/improve param names confirmed against 1.2.2).
# An unbounded >=1.0.0 risks the same "kwarg renamed under us" class of bug
# this file just got fixed for, on any future minor/patch bump.
#
# [groq,fastembed] extras: for the hosted deployment we replace Ollama with
# Groq (LLM) and fastembed (embeddings, runs in-process — no server to host).
# This does NOT touch memory_service.py's remember/recall/improve/forget
# calls; those are provider-agnostic. It only changes which env vars
# initialize() sets (LLM_PROVIDER=groq, EMBEDDING_PROVIDER=fastembed).
cognee[groq,fastembed]>=1.2.0,<1.3.0

# Ollama Integration — kept for local dev only (run.sh / local .env still
# use Ollama). Not required in the hosted/production environment.
ollama>=0.3.0

# Data Validation & Settings
pydantic>=2.10.5
pydantic-settings>=2.4.0

# File Handling
python-multipart>=0.0.22
aiofiles>=23.2.1

# Graph & Visualization
networkx>=3.4.2
plotly>=5.24.1
kaleido>=0.2.1

# Logging & Utilities
loguru>=0.7.2
rich>=13.7.1

# HTTP Client (for future use)
httpx>=0.27.0

# Development Tools
pytest>=8.2.2
pytest-asyncio>=0.23.7
